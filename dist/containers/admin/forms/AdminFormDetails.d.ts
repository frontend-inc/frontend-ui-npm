import React from 'react';
import { FormType } from '../../../types';
type AdminFormProps = {
    form: FormType;
    handleEdit: () => void;
};
export default function AdminFormDetails(props: AdminFormProps): React.JSX.Element;
export {};
