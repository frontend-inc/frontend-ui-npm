import React from 'react';
type FormWizardProgressProps = {
    currentStep: number;
    totalSteps: number;
};
export default function FormWizardProgress({ currentStep, totalSteps, }: FormWizardProgressProps): React.JSX.Element;
export {};
