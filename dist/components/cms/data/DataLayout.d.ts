import React from 'react';
type DataLayoutProps = {
    loading?: boolean;
    grid?: boolean;
    children: React.ReactNode;
};
export default function DataLayout({ loading, grid, children, }: DataLayoutProps): React.JSX.Element;
export {};
