import React from 'react';
type RegisterProps = {
    title?: string;
    subtitle?: string;
    redirectUrl: string;
    loginUrl: string;
};
declare const Register: React.FC<RegisterProps>;
export default Register;
