import React from 'react';
type LoadMoreProps = {
    loading?: boolean;
    hasNextPage?: boolean;
    handleSearch?: any;
};
declare const LoadMore: React.FC<LoadMoreProps>;
export default LoadMore;
