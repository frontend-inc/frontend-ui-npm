import React from 'react';
import { NotificationType } from '../../../types';
type NotificationProps = {
    notification: NotificationType;
};
export default function Notification({ notification }: NotificationProps): React.JSX.Element;
export {};
