import React from 'react';
import { NotificationType } from '../../../types';
type NotificationsProps = {
    notifications: NotificationType[];
};
export default function Notifications({ notifications }: NotificationsProps): React.JSX.Element;
export {};
