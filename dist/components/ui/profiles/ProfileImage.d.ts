import React from 'react';
type ProfileImageProps = {
    src: string;
    size?: number;
    disableRing?: boolean;
};
declare const ProfileImage: React.FC<ProfileImageProps>;
export default ProfileImage;
