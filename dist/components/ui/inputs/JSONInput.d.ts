import React from 'react';
import { InputPropsType } from '../../../types';
type JSONInputProps = InputPropsType;
export default function JSONInput({ errors, value, name, label, info, placeholder, handleChange, }: JSONInputProps): React.JSX.Element;
export {};
