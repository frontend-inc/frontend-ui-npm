import React from 'react';
import { LineItemType } from '../../../types';
type CartLineItemProps = {
    lineItem: LineItemType;
};
declare const CartLineItem: React.FC<CartLineItemProps>;
export default CartLineItem;
