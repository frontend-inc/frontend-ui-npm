import React from 'react';
type LayoutTabIconProps = {
    icon: string;
    selected?: boolean;
    handleClick?: () => void;
};
export default function LayoutTabIcon({ icon, selected, handleClick, }: LayoutTabIconProps): React.JSX.Element;
export {};
